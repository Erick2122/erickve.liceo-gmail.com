int x;
int y;

void setup(){
  size(600,600);
  background(0);
  x = 0;
  y = 75;
}

void draw(){
  while(x < 600){
    fill(#FFFFFF);
    rect(x,0,75,75);
    rect(y,75,75,75);
    x = x + 150;
    y = y + 150;
  }
}
